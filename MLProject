name: mlflow-demo

conda_env: conda.yaml

# Can have a docker_env instead of a conda_env, e.g.
# docker_env:
#    image:  mlflow-docker-example

entry_points:
  train:
    parameters:
      data_file: path
      alpha: {type: float, default: 1.0}
    command: "python mlflow/train.py -a {alpha} -d {data_file}"